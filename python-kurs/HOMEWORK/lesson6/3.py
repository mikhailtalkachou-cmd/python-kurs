def oblicz_srednia(*args: int) -> float:
    i = args
    for idx, ocena in enumerate(i, 1):
        ilosć_ocen = 0 + idx   
        idx += 1
    summa_ocen = sum(args)
    return i, ilosć_ocen, summa_ocen, summa_ocen / ilosć_ocen

print(oblicz_srednia(2, 1, 3, 3, 5, 4))

 